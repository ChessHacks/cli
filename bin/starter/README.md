# ChessHacks Starter Bot

This is a starter bot for ChessHacks. It includes a basic bot and devtools.

To start, install the required packages found in `requirements.txt`.

```bash
pip install -r requirements.txt
```

or whatever you'd like.
